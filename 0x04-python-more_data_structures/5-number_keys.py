#!/usr/bin/python3
def number_keys(a_dictionary):
    ''' Returns the number of keys in a dictionary 'a_dictionary'

    >>> a_dictionary = { 'language': "C", 'number': 13, 'track': "Low level" }
    >>> number_keys(a_dictionary)
    3

    '''

    return len(a_dictionary)
