-- Displaying the contents of the table second_table, just the score
-- and name fields with the output arranged in Descending order
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
